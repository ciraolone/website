<!DOCTYPE html>
<html lang="{{ site.language }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ description or site.description }}">
  <meta name="author" content="{{ site.author }}">
  
  <title>{% if title %}{{ title }} | {% endif %}{{ site.title }}</title>
  
  <!-- Preconnect per performance -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  
  <!-- CSS -->
  <link rel="stylesheet" href="/assets/css/main.css">
  
  <!-- Favicon -->
  <link rel="icon" type="image/x-icon" href="/favicon.ico">
</head>
<body class="flex flex-col min-h-screen">
  <header class="bg-blue-600 text-white shadow-lg">
    <nav class="container mx-auto px-4 py-4 flex justify-between items-center">
      <a href="/" class="logo text-2xl font-bold hover:text-blue-200">{{ site.title }}</a>
      <ul class="flex gap-6">
        <li><a href="/" class="hover:text-blue-200 transition-colors">Home</a></li>
        <li><a href="/chi-siamo/" class="hover:text-blue-200 transition-colors">Chi Siamo</a></li>
        <li><a href="/contatti/" class="hover:text-blue-200 transition-colors">Contatti</a></li>
      </ul>
    </nav>
  </header>

  <main class="container mx-auto px-4 py-8 prose prose-lg max-w-none flex-1">
    {{ content | safe }}
  </main>

  <footer class="bg-gray-100 text-gray-700 py-6 mt-12">
    <div class="container mx-auto px-4 text-center">
      <p>&copy; {{ "now" | dateFormat("year") }} {{ site.author }}. Tutti i diritti riservati.</p>
    </div>
  </footer>

  <!-- JavaScript -->
  <script src="/assets/js/main.js"></script>
</body>
</html>

