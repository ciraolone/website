{#
  Componente CardGrid - Griglia responsiva di card

  Parametri:
  - colsXs: numero colonne extra-small (default: 1)
  - colsSm: numero colonne small sm (default: 2)
  - colsMd: numero colonne tablet md (default: 3)
  - colsLg: numero colonne desktop lg (default: 3)
  - gap: gap tra le card (default: 6 = 1.5rem)
  - className: classi CSS aggiuntive per il container (opzionale)

  Uso:
  {% from "components/cardGrid.njk" import cardGrid %}
  {% call cardGrid() %}
    {{ card(...) }}
    {{ card(...) }}
  {% endcall %}

  Classi Tailwind (per purge, non cancellare):
  grid-cols-1 grid-cols-2 grid-cols-3 grid-cols-4 grid-cols-5 grid-cols-6
  sm:grid-cols-1 sm:grid-cols-2 sm:grid-cols-3 sm:grid-cols-4 sm:grid-cols-5 sm:grid-cols-6
  md:grid-cols-1 md:grid-cols-2 md:grid-cols-3 md:grid-cols-4 md:grid-cols-5 md:grid-cols-6
  lg:grid-cols-1 lg:grid-cols-2 lg:grid-cols-3 lg:grid-cols-4 lg:grid-cols-5 lg:grid-cols-6
  gap-2 gap-3 gap-4 gap-5 gap-6 gap-7 gap-8
#}

{% macro cardGrid(
  colsXs="1",
  colsSm="2",
  colsMd="3",
  colsLg="3",
  gap="6",
  className=""
) %}
{% set gridClass = "grid grid-cols-" + colsXs + " sm:grid-cols-" + colsSm + " md:grid-cols-" + colsMd + " lg:grid-cols-" + colsLg + " gap-" + gap %}
<div class="{{ gridClass }} {{ className }}">
{{ caller() }}
</div>
{% endmacro %}
